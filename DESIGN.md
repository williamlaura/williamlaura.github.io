# 全站统一设计规范（页面交集）

> 以下规范适用于**所有页面**。任何新增或修改的页面都必须遵守这些共同规则。

---

## 1. 全局配色（所有页面共用）

| 变量 | 色值 | 用途 |
|------|------|------|
| `$primary` | `#c9a87c` | 主色调：标题高亮、hover状态、强调元素 |
| `$body-bg` | `#fbf5e8` | 页面背景：所有内容页统一奶油色 |
| `$body-color` | `#4a4a4a` | 正文文字 |
| 辅助文字 | `#888` / `#999` | 副标题、说明文字 |

**禁止**：任何页面使用与上述冲突的独立背景色或文字色，除非该页面为特殊沉浸式场景（如欢迎页全屏大图）。

---

## 2. 全局字体（所有页面共用）

| 层级 | 字体栈 |
|------|--------|
| 英文标题 | `'Playfair Display', Georgia, serif` |
| 英文正文 | `'Lato', 'Helvetica Neue', Arial, sans-serif` |
| 中文正文 | `'PingFang SC', 'Hiragino Sans GB', 'Microsoft YaHei', 'Noto Sans SC', sans-serif` |
| 中文艺术/装饰 | `'LiSu', '隶书', 'STKaiti', 'KaiTi', serif`（仅限短句装饰） |

**禁止**：引入新的外部字体库；所有字体通过现有 Google Fonts CDN 或系统字体栈解决。

---

## 3. 全局组件（所有页面必须包含）

### 3.1 音乐播放器

- **位置**：固定定位，`right: 80px; bottom: 28px;`
- **行为**：
  - 页面首次用户交互（点击/触摸/按键/滚动/滚轮）后尝试自动播放
  - 播放时按钮持续旋转（`4s/圈`，顺时针）
  - 暂停时旋转停止
  - 信息卡片始终可见，显示当前曲名和艺术家
- **配置**：每个页面在 qmd 中通过隐藏 `music-meta` 元素声明本页音频路径
- **资源**：音频文件统一放 `music/` 目录，在 `_quarto.yml` 的 `resources` 中声明 `"music/*"`

### 3.2 导航栏

- **背景**：`#fbf5e8`，底部 `1px` 细线 `rgba(0,0,0,0.05)`
- **文字**：大写英文，字距 `1.5px`，字号 `0.85rem`，hover 变主色 `#c9a87c`
- **品牌名**：`Playfair Display`，字重 600，字距 `1px`
- **例外**：欢迎页 (`index`) 隐藏导航栏

---

## 4. 页面布局共同规则

| 规则 | 值 | 说明 |
|------|-----|------|
| 最大内容宽度 | `900px` | 正文区域不可超过此宽度，居中显示 |
| 水平边距（桌面） | `2rem` ~ `4rem` | 根据视口弹性调整 |
| 水平边距（移动） | `1.5rem` | 移动端缩小 |
| 行高（中文正文） | `1.7` | 保证中文阅读舒适度 |
| 行高（英文正文） | `1.6` | 英文紧凑但可读 |
| 字距（中文正文） | `≤ 0.05em` | 中文不可使用过大字距 |
| 字距（英文标签/缩写） | `4px ~ 8px` | 小字英文可适当放宽 |

---

## 5. 资源引用共同规则

| 资源类型 | 存放路径 | 引用方式 | 必要配置 |
|----------|----------|----------|----------|
| 音频 | `music/*.mp3` | 相对路径 `music/xxx.mp3` | `_quarto.yml` 的 `resources` 中声明 `"music/*"` |
| 图片 | `images/*` | 相对路径 `images/xxx.jpg` | 被 qmd 直接引用时自动复制 |
| 外部 CSS/字体 | CDN | `_quarto.yml` 的 `css` 字段 | 统一在配置中管理 |

**禁止**：在单个 qmd 页面中引入独立的外部样式或脚本；所有全局资源通过 `_quarto.yml` 或 `styles.scss` 统一管理。

---

## 6. 动画共同参数

| 动画 | 时长 | 缓动 | 适用场景 |
|------|------|------|----------|
| 淡入 (`fadeIn`) | `0.8s` | `ease-out` | 元素从无到有的基础出现 |
| 旋转 (`musicSpin`) | `4s/圈` | `linear` | 音乐播放时按钮无限旋转 |
| 音波 (`musicWave`) | `1.2s` | `ease-in-out` | 三根柱子交替跳动 |

**禁止**：任何动画使用 `translateY` 位移造成页面晃动；文字动画仅限 `opacity`、`transform: scale()`、`font-size` 变化。

---

## 7. 响应式共同断点

| 断点 | 宽度 | 主要调整 |
|------|------|----------|
| 移动端 | `≤ 900px` | 左右分栏改为垂直堆叠、字号缩小、间距收紧 |

---

## 8. 构建与验证

- 所有样式修改必须写入 `styles.scss`，**禁止**在 qmd 中使用内联 `style` 属性（原始 HTML 块中的 `img` 等除外）
- 修改后必须执行 `quarto render` 验证构建
- 构建输出目录为 `docs/`，不可更改

---

## 9. 新增页面检查清单

新增页面时，确认以下**交集项**已落实：

- [ ] 配色未偏离全局色值
- [ ] 字体使用现有字体栈，未引入新字体
- [ ] 导航栏正确显示（欢迎页除外）
- [ ] 右下角音乐播放器已配置（`music-meta` + 音频文件）
- [ ] 内容区域最大宽度 `900px` 且居中
- [ ] 中文正文字距 `≤ 0.05em`
- [ ] 资源路径符合统一规范
- [ ] `quarto render` 构建通过
